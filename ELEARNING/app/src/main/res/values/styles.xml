<resources>

    <!-- Base application theme. -->
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorSecondary">@color/color_ffffff</item>
    </style>
    <style name="BCCardView" parent="CardView">
        <item name="android:layout_margin">16dp</item>
        <item name="cardCornerRadius">@dimen/cardview_default_radius</item>
        <item name="cardElevation">@dimen/cardview_default_elevation</item>
    </style>
    <style name="FacebookLoginButton">
        <item name="android:textSize">@dimen/text_size_12</item>
        <item name="android:background">@drawable/icon_login_fb</item>
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="android:layout_gravity">center_horizontal</item>
    </style>
    <style name="BCEditText">
        <item name="android:background">@null</item>
        <item name="android:padding">@dimen/margin_10dp</item>
        <item name="android:layout_marginBottom">@dimen/margin_5dp</item>
    </style>
    <style name="checkBox">
        <item name="android:button">@drawable/custom_checkbox_background</item>
        <item name="android:padding">@dimen/margin_5dp</item>
    </style>
</resources>
